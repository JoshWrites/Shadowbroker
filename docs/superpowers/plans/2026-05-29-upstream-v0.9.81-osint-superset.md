# Upstream v0.9.81 OSINT Superset — Implementation Plan

> **For agentic workers:** Execute task-by-task. Each task ends in a buildable/verifiable state and a commit. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Bring upstream v0.9.81's OSINT/dashboard advances into Josh's branch as an additive superset, preserving all his custom features and unioning datasets, while excluding Infonet/Tauri/privacy-core subsystems.

**Architecture:** Selective additive integration on branch `upstream-merge-may-2026` (off `cobain-merge-april-1`). Bring in upstream files by targeted `git checkout upstream/main -- <path>` + adaptation, grouped logically, building/verifying after each group. Never blind-merge. Push to `origin` only.

**Tech Stack:** FastAPI + Python 3.12 (backend), Next.js 16 + React 19 + MapLibre (frontend), SQLite, Docker Compose.

**Reference:** Design at `docs/superpowers/specs/2026-05-29-upstream-v0.9.81-osint-superset-design.md`.

---

## Pre-flight invariants (hold for every task)
- Work only on `upstream-merge-may-2026`.
- Never `git push upstream`. Pushes (when asked) go to `origin`.
- After each backend group: `python -c "import main"`-equivalent import check from `backend/`.
- After each frontend group: it must still `next build` (or at least typecheck) by the end of the frontend phase.
- Pikud HaOref files are not modified unless an import they depend on is renamed by an in-scope change.
- Honor architecture rule: backend fetchers run always — if an in-scope fetcher arrives with `is_any_active()` per-fetch gating, leave stream-lifecycle gating intact but do not let it block per-fetch execution where Josh's design expects always-run. (Matches existing `_store.py` behavior.)

---

## Task 1: Bring in new self-contained OSINT fetchers (backend)

**Files (checkout from upstream/main):**
- `backend/services/fetchers/crowdthreat.py`
- `backend/services/fetchers/flight_observations.py`
- `backend/services/fetchers/nuforc_enrichment.py`
- `backend/services/fetchers/route_database.py`
- `backend/services/fetchers/usni_fleet_tracker.py`
- `backend/services/fetchers/wastewater.py`
- `backend/services/fetchers/aishub_fallback.py`
- `backend/services/fetchers/aircraft_database.py`

These depend only on `network_utils` (already present), `_store`, `retry`.

- [ ] **Step 1:** `git checkout upstream/main -- <each path above>`
- [ ] **Step 2:** Diff `_store.py` between mine and upstream; if these fetchers import names my `_store.py` lacks (e.g. a new helper), bring the minimal `_store.py` additions in without removing my behavior.
- [ ] **Step 3:** Import-check: from `backend/`, `python -c "import services.fetchers.crowdthreat, services.fetchers.flight_observations, services.fetchers.nuforc_enrichment, services.fetchers.route_database, services.fetchers.usni_fleet_tracker, services.fetchers.wastewater, services.fetchers.aishub_fallback, services.fetchers.aircraft_database"`. Expected: no ImportError.
- [ ] **Step 4:** Commit: `feat: add upstream OSINT fetchers (crowdthreat, flight_obs, nuforc, route_db, usni, wastewater, aishub, aircraft_db)`

## Task 2: Bring in SAR subsystem (self-contained, no crypto-core dep)

**Files:**
- Create dir `backend/services/sar/` (7 files: `__init__, sar_aoi, sar_catalog_client, sar_config, sar_normalize, sar_products_client, sar_signing`)
- `backend/services/fetchers/sar_catalog.py`, `backend/services/fetchers/sar_products.py`

- [ ] **Step 1:** `git checkout upstream/main -- backend/services/sar/ backend/services/fetchers/sar_catalog.py backend/services/fetchers/sar_products.py`
- [ ] **Step 2:** Grep the new SAR files for imports of excluded subsystems (`privacy`, `infonet`, `mesh`, `wormhole`). If any found, stub or pull the minimal module; document.
- [ ] **Step 3:** Import-check `services.fetchers.sar_catalog` and `services.fetchers.sar_products` from `backend/`. Expected: no ImportError.
- [ ] **Step 4:** Commit: `feat: add upstream SAR catalog/products subsystem`

## Task 3: Wire new fetchers into the scheduler/data_fetcher

**Files:** `backend/services/data_fetcher.py` (Modify), `backend/main.py` (Modify if it registers fetchers/endpoints)

- [ ] **Step 1:** Diff upstream vs mine for `data_fetcher.py`. Identify the import lines + scheduler registrations for the Task 1/2 fetchers only (ignore Infonet/mesh registrations).
- [ ] **Step 2:** Add those imports + scheduled jobs into my `data_fetcher.py`, preserving my Pikud/Ukraine/Cloudflare registrations and my always-run behavior.
- [ ] **Step 3:** If new fetchers expose endpoints in upstream `main.py`, graft just those route handlers (skip routes for excluded subsystems).
- [ ] **Step 4:** Import-check: from `backend/`, import the app module. Expected: app imports without error.
- [ ] **Step 5:** Commit: `feat: register new OSINT fetchers in scheduler + routes`

## Task 4: Union datasets

**Files:** `backend/data/plane_alert_db.json` (take upstream), keep `military_bases.json` (mine), plus any diverging train/route data; gain in-scope upstream-only data files.

- [ ] **Step 1:** `git checkout upstream/main -- backend/data/plane_alert_db.json` (16,007 entries).
- [ ] **Step 2:** For each diverging dataset, write a count assertion: merged entry count >= max(mine, theirs) and every key present in either is present in result. For simple supersets (plane_alert, military_bases) this is "take the larger / keep mine deliberately."
- [ ] **Step 3:** Bring in upstream-only data files required by Task 1/2 fetchers (e.g. any csv/json those fetchers read locally). Skip `release_digests.json`, `tor_bundle_digests.json` (excluded-subsystem).
- [ ] **Step 4:** Run a quick Python count check per dataset; print before/after counts.
- [ ] **Step 5:** Commit: `data: union datasets (plane_alert 16007, keep military_bases 647, +in-scope data files)`

## Task 5: Frontend — new OSINT layers, panels, popups (adapt to my Context store)

**Files:** new components under `frontend/src/components/` (popups: WastewaterPopup, SigintPopup; SAR modals; TimelinePanel/Scrubber; LocateBar; relevant utils), `MaplibreViewer.tsx`, `WorldviewLeftPanel.tsx`, `page.tsx`, `types/dashboard.ts`.

**Rule:** keep my `DashboardDataProvider` Context + demand-driven + all-layers-OFF. New layers added as OFF-by-default. Components upstream wrote against `useDataStore`/`useDataKey` get adapted to `useDashboardData()`; if a component is not OSINT-essential and is deeply store-coupled, defer it (document).

- [ ] **Step 1:** Identify in-scope new frontend files (OSINT layers/popups/timeline/locate). Exclude InfonetTerminal, MeshChat, AIIntelPanel, Companion, SAR only if it pulls excluded crypto UI (SAR map layer OK).
- [ ] **Step 2:** Checkout those files; for each that imports `useDataStore`/`useDataKey`, rewrite to consume my Context (`useDashboardData()`); for `types/dashboard.ts`, merge new layer keys into my `ActiveLayers` (default false).
- [ ] **Step 3:** Add new layer toggles to `WorldviewLeftPanel.tsx` and new layer keys to `page.tsx` activeLayers init (all false) + polling layer CSV.
- [ ] **Step 4:** `cd frontend && npm run build` (or `npx tsc --noEmit`). Fix type errors from partial component set; defer non-essential store-coupled components.
- [ ] **Step 5:** Commit: `feat: add upstream OSINT map layers/panels adapted to Context store`

## Task 6: Security/bugfix backports (in-scope only)

**Files:** targeted fixes (e.g. defusedxml for untrusted XML parsing in fetchers, paho-mqtt pin if meshtastic fetcher needs it, AIS SPKI pinning if AIS fetcher in scope).

- [ ] **Step 1:** From the upstream security commits, select only those touching in-scope fetchers/web/backend (not Infonet/mesh-crypto). 
- [ ] **Step 2:** Apply each; verify import/build still passes.
- [ ] **Step 3:** Commit: `fix: backport in-scope upstream security/bugfixes`

---

## TEST PLAN

Run after build phase. Each maps to a success criterion in the spec.

### T1 — Backend imports & starts (no excluded-subsystem errors)
- From `backend/`: import the FastAPI app module. Expected: no ImportError, no reference to infonet/tauri/privacy_core.
- Grep the running import graph: `grep -rE "import.*(infonet|src-tauri|privacy_core)" backend/services/fetchers backend/services/sar` → expected: no matches in in-scope code.

### T2 — Frontend builds
- `cd frontend && npm run build`. Expected: exit 0.

### T3 — App runs (Docker)
- `docker compose up -d --build`; wait for health; `curl -s localhost:<port>/` returns the dashboard; `docker compose logs` shows no fatal import errors.

### T4 — Josh's features intact
- Pikud: `curl /api/live-data/fast` (or the pikud endpoint) returns pikud_alerts structure; pikud fetcher file unchanged (`git diff cobain-merge-april-1 -- backend/services/fetchers/pikud_haoref.py` empty).
- Ukraine: history/range endpoints respond.
- Cloudflare Radar: fetcher present and scheduled.
- listener/: dir intact, unchanged.

### T5 — Demand-driven / all-layers-off
- Inspect built `page.tsx` activeLayers init: every layer (incl. new upstream ones) initialized `false`.
- Confirm polling sends `?layers=` param (demand-driven preserved).

### T6 — New fetchers load & degrade gracefully
- Import each new fetcher; call its fetch fn with no API key set → expected: returns empty/handled, no crash. (Where a key exists in `.env`, it's used.)

### T7 — Datasets unioned, nothing lost
- `plane_alert_db.json` >= 16,007 entries; `military_bases.json` == 647 (kept mine); for any train/route union: merged >= max(sides).

### T8 — Excluded subsystems absent
- `test ! -d backend/services/infonet && test ! -d src-tauri && test ! -d frontend/src/mesh/privacyCoreWasm` (or confirm none were added by this branch). 
- `git diff --name-only cobain-merge-april-1..HEAD | grep -iE "infonet|src-tauri|privacy_core|privacyCore"` → expected: empty.

### T9 — Upstream untouched
- `git log upstream/main` unchanged; no commits pushed to upstream remote (only origin push when explicitly done).

### Pass condition
All T1–T9 pass → mark build verified → delete backup zip.
