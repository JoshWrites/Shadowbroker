# Signal Archive — environment variables
# Copy to .env and adjust as needed

# Port the backfill API listens on (default: 7654)
ARCHIVE_PORT=7654

# Ukraine alerts.in.ua bearer token (register at devs.alerts.in.ua)
# Required for the ukraine collector — leave blank to disable
UKRAINE_ALERTS_TOKEN=
